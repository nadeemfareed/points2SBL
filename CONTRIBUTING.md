# Contributing

Use a clean environment, add tests for behavior changes, and run
`tools/audit_release.ps1` before opening a pull request. Do not commit point
clouds, training blocks, checkpoints, local run folders, credentials, or
machine-specific paths.
