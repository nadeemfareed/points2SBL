# Pretrained model

The repository keeps the expected checkpoint directory structure but does
not distribute model binaries through Git source control.

Place the released Point Transformer checkpoint at:

runs/point_transformer_curated_20260327_170108/best.pt

The checkpoint can also be installed with the points2SBL model manager once
the corresponding GitHub release asset is published.
