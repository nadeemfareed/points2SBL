# conda-forge submission

Publish version 0.3.0 to PyPI first. Copy the SHA256 of the PyPI source
distribution into `recipe.yaml`, then submit the recipe directory to
`conda-forge/staged-recipes`. The final feedstock is created by conda-forge
after review/merge.
